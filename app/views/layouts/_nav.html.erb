<nav class="navbar">
  <div class="container-fluid">
    <% if current_user %>
      <div class="collapse navbar-collapse login" id="navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li class="name"> Welcome, <%= current_user.name %></li>
          <li class="logout-nav"> <%= link_to 'Log Out', logout_path, method: :delete %></li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>